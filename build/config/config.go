package config

const (
	// ContainerRegistryName is the name of the container registry to use when released images are pushed.
	ContainerRegistryName = "ghcr.io"

	// ContainerRegistryOrgName is the organization name to use when released images are pushed.
	ContainerRegistryOrgName = "tokenize-x"

	// ZNetVersion is the version used in tags for images built for znet.
	ZNetVersion = "znet"
)
